[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
engine = { path = "../engine" }
dioxus = { version = "0.7.9", features = ["desktop", "mobile"] }
tokio = { version = "1.53.1", features = ["full"] }
dirs = "6.0.0"
jni = "0.22.4"
ndk-context = "0.1.1"
log = "0.4.33"

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.21"
ndk-context = "0.1"